# Threshold — public pages

Two static pages, nothing else:

- `index.html` — try Threshold: enter three weeks of encounters and get a verdict.
- `privacy.html` — the privacy policy, generated from the app's data inventory.

Everything runs in your browser. Nothing you type is uploaded anywhere.

The app source is not in this repository.
